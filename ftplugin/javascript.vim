" This is for the most part nice, but it is a huge performance penalty for large
" javascript files.  I may want to do this only if the file is less than 1
" kiloline or something.
"
" set foldmethod=syntax

" Why is this not already in vim-javascript?
set suffixesadd+=.js
